<!DOCTYPE html>
<html lang="en">

<head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Notes App</title>
      <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="min-h-screen bg-zinc-900 flex items-center justify-center p-6">
      <div class="w-full max-w-2xl space-y-6">

            <!-- Form -->
            <div class="bg-zinc-800 p-6 rounded-2xl shadow-lg border border-zinc-700">
                  <form action="/create" method="post" class="flex flex-col gap-4 text-white">
                        <input type="text" placeholder="Title" name="title"
                              class="px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-green-500 placeholder-zinc-400">

                        <textarea placeholder="Write your notes" name="notes"
                              class="px-4 py-2 h-32 rounded-lg bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-green-500 placeholder-zinc-400 resize-none"></textarea>

                        <input type="submit" value="Add Note"
                              class="bg-green-600 hover:bg-green-500 transition-colors w-32 h-11 rounded-lg cursor-pointer font-medium shadow-md">
                  </form>
            </div>

            <!-- Cards Section -->
            <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
                  <!-- Example Card -->
                  <!--  ejs working -->
                  <% if(files.length> 0){%>

                        <% files.forEach( function(val){ %>
                              <% const fileName = val.split('.')[0].split('_').join(" "); %>
                              <div
                                    class="  bg-zinc-800 p-5 rounded-xl shadow-md border border-zinc-700 hover:shadow-lg transition">
                                    <h1 class="text-xl font-semibold text-white mb-3"><%= fileName %></h1>
                                    <a href="/<%= val %>" class="text-green-500 hover:underline font-medium">
                                          Read more →
                                    </a>
                              </div>
                              <% })%>


                                    <% } else{ %>
                                          <h1 class="text-green-500 font-medium text-center">
                                                no task yet
                                          </h1>
                                          <% } %>


            </div>

      </div>
</body>

</html>